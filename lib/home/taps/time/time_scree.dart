import 'package:flutter/material.dart';

class TimeScree extends StatelessWidget {
  const TimeScree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}